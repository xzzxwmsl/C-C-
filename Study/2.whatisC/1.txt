C语言中的六种语句：标号语句，复合语句，表达式语句，选择语句，迭代语句，跳转语句。
key： 关键字，标识符，运算符，数据
#include<stdio.h>  为预处理指令，相当于把 stdio.h 文件中的所以内容copy到此处

变量命名： 小写字母，大写字母，下划线，数字。不能以数字开头，区分大小写

在C99中main函数若省略return， 函数末尾会自动return 0；
